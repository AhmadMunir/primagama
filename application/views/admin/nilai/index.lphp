<html>
  <head>
    <title>IMPORT EXCEL</title>
  </head>

  <body>
    <a href="<?php echo base_url("admin/nilai/form"); ?>">IMPORT data</a>

    <br><br>

  <table border="1" cellpadding="8">
  <tr>
    <th>NIS</th>
    <th>Nama</th>
    <th>Jenis Kelamin</th>
    <th>Alamat</th>
  </tr>

  <?php
  if( ! empty($siswa)){ // Jika data pada database tidak sama dengan empty (alias ada datanya)
    foreach($siswa as $data){ // Lakukan looping pada variabel siswa dari controller
      echo "<tr>";
      echo "<td>".$data->id."</td>";
      echo "<td>".$data->id_siswa."</td>";
      echo "<td>".$data->nilai."</td>";
      echo "<td>".$data->id_mapel."</td>";
      echo "<td>".$data->try_out_ke."</td>";
      echo "</tr>";
    }
  }else{ // Jika data tidak ada
    echo "<tr><td colspan='4'>Data tidak ada</td></tr>";
  }
  ?>
  </table>
  </body>
</html>
